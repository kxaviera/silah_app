# ============================================
# Silah Backend - VPS .env (updated)
# Path: /var/www/silah_app/backend/.env
# Replace placeholder values with your real ones
# ============================================

NODE_ENV=production
PORT=5000
API_URL=https://api.rewardo.fun

# Database (replace password if needed)
MONGODB_URI=mongodb+srv://silah_db_user:YOUR_MONGODB_PASSWORD@silah.paytsil.mongodb.net/?appName=silah

# JWT - use full 64-char hex strings, no spaces or > at end
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=PASTE_FULL_64_CHAR_HEX_HERE
JWT_EXPIRE=7d
ADMIN_JWT_SECRET=PASTE_FULL_64_CHAR_HEX_HERE
ADMIN_JWT_EXPIRE=24h

# Uploads
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880

# CORS & URLs
CORS_ORIGIN=https://admin.rewardo.fun,https://api.rewardo.fun
FRONTEND_URL=https://admin.rewardo.fun

# Admin default (for create-admin script; change password in production)
ADMIN_DEFAULT_EMAIL=admin@rewardo.fun
ADMIN_DEFAULT_PASSWORD=Secure@123

# Firebase (FCM) - optional; uncomment and set if you use push notifications
# Option A: path to service account JSON on VPS
# GOOGLE_APPLICATION_CREDENTIALS=/var/www/silah_app/backend/firebase-service-account.json
# Option B: env vars
# FIREBASE_PROJECT_ID=silah-app-e0bb8
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@silah-app-e0bb8.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Optional: Google Sign-In
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret

# Optional: Stripe
# STRIPE_SECRET_KEY=sk_live_xxx
# STRIPE_PUBLISHABLE_KEY=pk_live_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx

# Optional: Email (SendGrid)
# SENDGRID_API_KEY=SG.xxx
# SENDGRID_FROM_EMAIL=noreply@rewardo.fun

# Optional: SMS (Twilio)
# TWILIO_ACCOUNT_SID=ACxxx
# TWILIO_AUTH_TOKEN=xxx
# TWILIO_PHONE_NUMBER=+1234567890

# Socket.io (usually same as frontend)
SOCKET_IO_CORS_ORIGIN=https://admin.rewardo.fun
